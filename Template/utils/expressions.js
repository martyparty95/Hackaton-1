// easy

const isMinLength = (str, minLength) => null; 
const result1 = utils.isMinLength('hello', 5);
const result2 = utils.isMinLength('world', 10);

console.log(result1); 
console.log(result2); 


const isMaxLength = (str, maxLength) => null; // TODO

// medium

const isIn = (value, possibleValues) => null; // TODO

const isArrayOfType = (arr, type) => null; // TODO

// hard

const areValidNumbers = (stringNumbers) => null; // TODO

const sumNumbersFromString = (string) => null; // TODO

export {
  isMinLength,
  isMaxLength,
  isIn,
  isArrayOfType,
  areValidNumbers,
  sumNumbersFromString,
};
